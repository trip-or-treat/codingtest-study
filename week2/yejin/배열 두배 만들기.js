const solution = (numbers) => {
  return numbers.map((num) => num * 2);
};

// array.map(function(현재 요소, index, array)) : 배열 요소들을 인자로 받아 함수를 실행한다.
// 반복문의 효과가 있다.
// 현재 요소의 이름은 임의로 정한다.
// index, array 인자는 생략가능
